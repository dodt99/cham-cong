"use client";

/**
 * @deprecated Use `useAttendanceHydrated` and subscribe via `useAttendanceZustandStore` + selectors.
 */
export { useAttendanceHydrated as useAttendanceStore } from "@/hooks/use-attendance-hydrated";
